{
  "name": "frontenddesign",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply fitstrong-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply fitstrong-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "typescript": "^5.7.2",
    "wrangler": "^4.4.0"
  }
}
